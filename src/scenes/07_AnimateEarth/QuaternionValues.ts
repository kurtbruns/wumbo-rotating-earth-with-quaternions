import { Quaternion } from '@kurtbruns/vector';

export const RIGHT_Q_VALUE = new Quaternion(-0.2232, 0.4233, 0.3139, 0.8201).normalize();
export const LEFT_Q_VALUE = new Quaternion(0.88, 0.14, -0.17, 0.41).normalize();
